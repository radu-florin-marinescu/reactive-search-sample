<?xml version="1.0" encoding="utf-8"?>
<layout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="@dimen/space.L"
            android:paddingTop="@dimen/space.3XS"
            android:paddingEnd="@dimen/space.L"
            android:paddingBottom="@dimen/space.3XS"
            tools:background="@color/background.gradient.end"
            tools:ignore="ContentDescription">

        <ImageButton
                android:id="@+id/buttonSearch"
                android:layout_width="@dimen/toolbar.itemSize"
                android:layout_height="@dimen/toolbar.itemSize"
                android:background="@drawable/bg_button_light_rounded_corners"
                android:padding="@dimen/_12sdp"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_search"
                android:tint="@color/accent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toStartOf="@+id/buttonPortfolio"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

        <ImageButton
                android:id="@+id/buttonPortfolio"
                android:layout_width="@dimen/toolbar.itemSize"
                android:layout_height="@dimen/toolbar.itemSize"
                android:background="@drawable/bg_button_transparent_rounded_corners"
                android:padding="@dimen/_12sdp"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_portfolio"
                android:tint="@color/grey"
                app:layout_constraintBottom_toBottomOf="@+id/buttonSearch"
                app:layout_constraintEnd_toStartOf="@+id/buttonProfile"
                app:layout_constraintStart_toEndOf="@+id/buttonSearch"
                app:layout_constraintTop_toTopOf="@+id/buttonSearch" />

        <ImageButton
                android:id="@+id/buttonProfile"
                android:layout_width="@dimen/toolbar.itemSize"
                android:layout_height="@dimen/toolbar.itemSize"
                android:background="@drawable/bg_button_transparent_rounded_corners"
                android:padding="@dimen/_12sdp"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_profile"
                android:tint="@color/grey"
                app:layout_constraintBottom_toBottomOf="@+id/buttonSearch"
                app:layout_constraintEnd_toStartOf="@+id/buttonItems"
                app:layout_constraintStart_toEndOf="@+id/buttonPortfolio"
                app:layout_constraintTop_toTopOf="@+id/buttonSearch" />

        <ImageButton
                android:id="@+id/buttonItems"
                android:layout_width="@dimen/toolbar.itemSize"
                android:layout_height="@dimen/toolbar.itemSize"
                android:background="@drawable/bg_button_transparent_rounded_corners"
                android:padding="@dimen/_12sdp"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_items"
                android:tint="@color/grey"
                app:layout_constraintBottom_toBottomOf="@+id/buttonSearch"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintHorizontal_bias="0.5"
                app:layout_constraintStart_toEndOf="@+id/buttonProfile"
                app:layout_constraintTop_toTopOf="@+id/buttonSearch" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>